# Secrets — never commit Wi-Fi / API / OTA credentials
secrets.yaml

# Personal source-build wrapper (names, secrets and local overrides)
waveshare-va.local.yaml

# ESPHome build cache & artifacts
.esphome/
*.bin

# Python
__pycache__/
*.pyc

# OS junk
.DS_Store
Thumbs.db
desktop.ini
